# 実装指示：特別販売の表示は案A（タブ型）で確定・切替UIを削除（2026-08-05）

対象：`index.html`。lp.htmlは触らない。
竹内さん決定：特別販売の表示案は**A＝タブ型（週メニュー付随）で確定**。デモ用のA/B切替UIは役目を終えたので削除する。

作業前に必ず `git pull`。1タスク=1コミット。

## 仕様

1. `S.specialMode` は常に 'A' 相当の挙動に固定（state項目ごと廃止してよい。参照箇所をすべて整理）
2. **デモ用のA/B切替ボタン（画面右上／SPは左下退避の「表示案 A:タブ型 B:独立型」チップ・index.html:1408付近）を削除**。関連CSS（:130付近の退避スタイル）も削除
3. **案B（独立ページ型）のコードを削除**：specialPage のページ実装（:1358付近）・案B専用の説明文データ（:380付近）・`S.page==='specialPage'` の分岐等、案B専用の残骸が残らないように（git履歴に残るので復元可能）
4. 案A（タブ型）の特販表示自体は現状維持。タブ・一覧・詳細の動作が変わらないこと
5. 削除で他のデモトグル（表示地域・新規特典）やレイアウトが崩れないこと

## 検証観点（実装後に自分でも確認）

- 画面右上（SP左下）に「表示案」チップが出ない
- 特別販売タブ（タブ型）が従来どおり動く：一覧表示・商品詳細・カート投入
- 「specialPage」「specialMode」がコード内に残っていない（grepで0件）
- 他の開発用チップ（表示地域・新規特典判定）は残っている

## コミット

`fix: 特別販売の表示を案A（タブ型）に確定しA/B切替デモを削除`

## 完了報告の形式

変更概要／確認手順／コミットハッシュ。
