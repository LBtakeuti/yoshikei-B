# 実装指示：エリア確認モーダルに「案内ステップS」を追加（2026-08-05）

対象：`index.html` の Area モジュール。lp.htmlは触らない。
デザイン原本：`docs/snippets/AreaCheckModal.dc.html`（最新版に更新済み・未コミット。同コミットに含める）。状態Sの追加分。

作業前に必ず `git pull`。1タスク=1コミット。

## 仕様（竹内さん指示＋デザイン原本のSペイン）

未ログインのユーザーが**カートに反映（＋1）した起点でモーダルが開くときだけ**、郵便番号入力（A）の前に案内ステップSを挟む：

- 見出し「お買い物には会員登録が必要です」（改行は「お買い物には」の後。スタイルは既存titleと同一）
- リード「ヨシケイ楽楽webへの会員登録が必要です。まずは配達可能かお調べします。」
- マスコットは手振り（mascot-wave.png）
- プライマリボタン「進む」→ 状態Aへ
- 閉じる×は従来どおり（中断・カート投入されない）

### 起点による出し分け

- **カート投入起点**：S → A → B/C/D（今回の追加）
- **バナー・ピル起点**：従来どおりAから開始（買い物文脈ではないため案内は出さない）
- Dから「別の郵便番号を入力する」で戻る先はA（Sには戻らない）

## 回帰注意

- 既存のA〜D・カート投入続行・rk_area_checked保存・setRegion連動は変更しない
- カート投入起点でエリア確認済みの場合はモーダル自体出さない（従来どおり）

## コミット

`feat: カート起点のエリア確認に会員登録の案内ステップを追加`

## 完了報告の形式

変更概要／確認手順（カート起点=S表示・バナー起点=A直行・S→進む→A→B→投入続行）／コミットハッシュ。
