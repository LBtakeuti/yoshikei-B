# 実装指示：CMセクションの配置修正（2026-08-06）

対象：`index.html` のCMセクション（8a158e0で実装済み）。
竹内さん指示：**PC=2本を横並び、SP=縦積み**。現在PCも縦積みになっているのを修正する。

- PC（1024px以上）：`.cm-list` を2カラムの横並びに（grid 1fr 1fr・gap 16〜20px程度）。セクション全体の最大幅は他のPCセクションに合わせて広げてよい（各動画は16:9維持）
- SP：現状どおり縦積み（変更なし）
- ファサード方式・再生状態保持・タイトル表示は変更しない

検証：PC 1280で2本が横に並ぶ／SP 390で縦のまま／クリック再生・回帰OK

コミット：`fix: CMセクションをPC横並びに修正`
